#include<stdio.h>

int main() {

    int n,h;
    float p;
    double ts;
    scanf("%d %d %f",&n, &h, &p);
    ts = h*p;
 printf("NUMBER = %d", n);
    printf("SALARY = U$ %0.2f",ts);

return 0;
}
